<view>
  <block>
    <button 
    type="primary" 
    wx:for="{{pathList}}" 
    wx:key="item"
    data-item="{{item}}"
    bindtap="handleButtonTap"
    >
    {{item.name}}
    </button>
  </block>
</view>